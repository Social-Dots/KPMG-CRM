import { mockAPI } from './mockData';

// Always use mock API to prevent base44 redirects
export const Property = mockAPI.Property;
export const RentalApplication = mockAPI.RentalApplication;
export const Client = mockAPI.Client;
export const Lease = mockAPI.Lease;
export const User = mockAPI.User;